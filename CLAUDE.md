# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 읽는 프로젝트 안내서입니다.
**팀원 전원이 같은 맥락에서 작업하도록** 확정 사항과 금지 사항을 여기 모읍니다.

---

## 프로젝트

**멋쟁이사자처럼 Campus AX-Ton 강원대 · 팀 프로젝트**
팀명: **멋쟁이 밤티들** / 서비스명: **복지그루**

이름은 「복지」와 나무 한 그루의 「그루」를 붙인 것이다. 부처마다 갈라져 있는 지원제도를
한 그루로 모아 담당자와 학부모가 **같은 한 장**을 본다는 뜻이다.

> 기관 담당자가 학부모 문의를 받았을 때, 아동 조건을 넣으면 **어디에 · 무엇을 들고 · 언제까지** 가야
> 하는지를 한 장으로 정리해 학부모에게 그대로 건넬 수 있게 만드는 업무 도구.

**조건 입력 → 규칙이 정리 → 3층 출력**

```
① 학부모용 안내문        ← 제품. 그대로 건넨다
② 건네기 전에 짚을 것     ← 담당자가 읽는 유일한 곳 (몇 줄)
③ 자세히 보기 (접힘)     ← 학부모가 파고들 때만
```

### 가치 명제 — 2026.08 담당자 인터뷰로 바뀌었다

| ❌ 아닌 것 | ✅ 맞는 것 |
|---|---|
| 담당자가 문의를 3분에 끝낸다 | **학부모가 한 번에 갖춰 오게 만들어 반송과 재문의를 줄인다** |

「다른 곳에 문의하세요」는 담당자에게 **5초짜리 행동**이라 도구가 더 빠르게 만들 수 없다. 진짜 부담은
**다른 소관 자료(진단서)가 없는 채로 신청이 들어와 되돌려 보내고 다시 받는 것**이다.
그래서 담당자가 소관 밖 제도를 **설명하게 만들지 않는다** — 그건 업무 확대다. 설명은 종이가 한다.

### 일정

| 시점 | 할 일 |
|---|---|
| ~2026-08-28 | 교육 종료 · **1차 발표** |
| 2026-09 1~2주 | 권역별 아이디어톤 (기획안 + 발표) |
| 10월 | 우수팀 한정 집중 멘토링 (프로토타입 구현) |
| 11월 | 전국 해커톤 |

**아이디어톤 산출물은 기획안 + 발표다.** 공고 문구가 *"AI 기반 서비스 기획안 도출 및 발표"*이고,
프로토타입 구현은 10월 우수팀 과제다. 구현 완성도보다 문제 정의와 근거가 점수다.

---

## 확정된 것

### 문제 정의 — 세 겹으로 갈라져 있다

| 무엇이 갈라져 있나 | 근거 |
|---|---|
| **부처가 갈려 있다** — 교육·복지·의료·고용 | 소관별 신청처 상이 |
| **같은 교육청 안에서도 담당자가 갈려 있다** | 강원 지침 원문: *"배치 결과를 반드시 치료업무, 방과후업무 담당자에게 공유"* |
| **제도 이름이 지역마다 다르고 시간이 지나면 또 바뀐다** | 카드 명칭 6개 시도 대조 · 장애등급제 폐지(2019) 용어 잔존 |

### 사용자 구조 — B2G2C

| | 누구 |
|---|---|
| 구매자 | **시도교육청 / 교육지원청** (B2G) |
| 사용자 | 기관 담당자 (특수교육지원센터 등) |
| 수혜자 | 학부모 — 무료. 안내문으로 도달 |

교육청은 위탁이 아니라 **직할**이다. 특수교육지원센터는 법인격이 없어 계약 주체가 될 수 없다
(특수교육법 제11조). **1개 시도교육청이 도입하면 산하 교육지원청 전부가 사용자가 된다.**

### 1면에는 확실한 것만 올린다

「건네기 전에 짚을 것」은 담당자가 읽는 **유일한 곳**이다. 그래서 두 가지를 지킨다.

1. **늘 뜨는 일반 안내는 올리지 않는다** — `crossTrack`(별개 절차) · `easyToMiss`(3지망)는
   조건과 무관하게 뜨므로 「자세히 보기」로 내린다
2. **근거를 확인하지 못한 경고도 올리지 않는다** — `Warning.verified === false` 면 접힘으로.
   「공식 목록에는 없지만 겹칠 수 있다」 수준을 1면에 두면 담당자가 그 숫자를 안 보게 된다

실측 — 프리셋 7개 전부에서 **1면 경고가 0건 또는 1건**이다. 새 경고를 넣을 때 이 숫자를 확인한다.

### 설계 원칙 — 코드와 화면 양쪽에서 지킨다

1. **판정하지 않는다** — "확인하세요"까지만. 최종 판단은 담당자
2. **개인정보를 저장하지 않는다** — 이름·연락처를 받지 않고, 입력값을 기록하지 않는다
3. **근거를 반드시 붙인다** — 모든 항목에 법령·지침 출처. 확인 못 한 것은 「예시」로 표시
4. **담당자 업무를 늘리지 않는다** — 상담 중 어차피 묻는 것만 입력, 결과는 즉시
5. **옛 이름도 함께 보여준다** — 담당자가 지침과 대조할 수 있게

### AI의 역할 — 규칙이 고르고 AI가 쓴다

**제도를 고르는 것은 규칙이다.** 같은 조건에 같은 결과가 나와야 담당자가 신뢰한다.
AI는 규칙이 못 하는 두 가지만 한다.

| 라우트 | 하는 일 | 검색 |
|---|---|---|
| `app/api/letter` | 학부모용 안내문 문장화 | ❌ 끔 |
| `app/api/lookup` | 우리 데이터에 없는 칸을 웹에서 찾기 | ⭕ Google Search grounding |

`lookup`은 **출처 URL이 없으면 답을 내지 않는다.** 결과에는 늘 「확인 필요」가 붙고 DB를 덮지 않는다.

`letter`는 조건만 받아 **서버에서 규칙을 다시 돌리고** 그 결과만 제미나이에 넘긴다.
클라이언트가 보낸 문장을 AI에 그대로 넘기지 않는다. 생년월일·상세 메모·담당자 연락처는 AI에 보내지 않는다.

---

## ⛔ 만들지 말 것 (이유가 있다)

| 금지 | 이유 |
|---|---|
| **자격 판정** | ⚖️ 잘못된 확정 안내는 실제 피해. 설계 원칙 1번 |
| **서류 대신 작성·제출** | ⚖️ **행정사법** — 무자격자의 행정기관 제출 서류 대행은 처벌 대상. "서류 목록과 서식 위치 안내"까지만 |
| **아동 명단·이력 저장** | 개인정보 처리 요건이 도입 장벽이 된다. 저장하지 않는 것이 판매 논리 |
| **상담 중 실시간 웹 검색으로 제도 확정** | 환각·노후 정보. 검색은 「빈칸 찾기」로만, 확정은 안 함 |
| **로그인·계정** | 필요 없고 개인정보 원칙과 충돌 |
| **AI가 제도를 고르게 하기** | 빠뜨리거나 없는 제도를 만든다. 재현성이 깨진다 |

한 줄: **"규칙이 고르고, AI는 못 하는 것만 한다."**

---

## 표현 규칙

| ❌ 쓰지 말 것 | ✅ 대신 |
|---|---|
| "받을 수 있습니다" | **"신청할 수 있습니다"**, **"확인이 필요합니다"** |
| "자동으로 판정합니다" | **"확인 목록을 드립니다"** |
| "서류를 써드립니다" | **"준비 서류와 서식 위치를 안내합니다"** |
| "AI가 제도를 찾아줍니다" (단독) | **"규칙이 고르고, 빈칸은 AI가 찾아 출처와 함께 보여줍니다"** |

---

## 범위 — 지금 구현된 것과 추후

**지금 구현된 지역은 강원·경남 두 곳이다.** 나머지 4개 시도는 지침을 대조해 「절차는 같고
명칭·서식만 다르다」는 것만 확인했고 **구현하지 않았다.**

| | 어디에 반영돼 있나 |
|---|---|
| `Region.implemented` | `gangwon` · `gyeongnam` 만 `true` |
| `IMPLEMENTED_REGIONS` | 화면 선택지와 `parseConditions` 가 이것만 받는다 |
| 화면 | 나머지 5개는 `disabled` + 「(대조만)」 표시 |
| `POST /api/sheet` · `/api/letter` | 그 밖의 지역은 **422** |
| `GET /api/reference` | **6개 전부 낸다** — 대조는 근거이므로 막지 않는다 |
| 참고 자료 화면 | 지역마다 「구현」/「대조만」 배지 |

확인하지 않은 서식 번호로 시트를 내면 「미확인」으로 가득한 반쯤 만든 결과가 나간다.

**새 지역을 열 때 확인할 것 두 가지다.**
1. `FORM_NO` 에 서식 번호를 채운다
2. **그 서류를 누가 쓰는지 확인한다.** 지역마다 다르면 `FORM_SOURCE_BY_REGION` 에 덮어쓴다 —
   강원의 기초조사 카드는 담임교사용이 있어 **학교 몫**이지만 경남의 기초조사서는 신청처에서
   받아 쓴다. 이걸 놓치면 「담임교사용을 학교에 요청하세요」라고 잘못 안내하게 된다

그다음 `implemented: true` 로 바꾼다.
구현 순서와 이유는 [기획서 6장](docs/복지그루-기획서.md)에 있다.

---

## ⚠ 아직 확정 안 된 것

1. **지역 자체사업 0건** — `LOCAL_PROGRAMS`가 비어 있다. 강원(원주) 기준으로 조사해야 한다.
   화면이 「조사할 곳」 목록을 띄우고 있다. **1순위 과제**
2. **교육 트랙 3개가 아직 「예시」** — 치료지원 · 방과후 교육활동 · 통학 지원.
   금액과 절차가 시도마다 달라 강원 지침 원문에서 확인해야 한다
3. **구매 구조 미확인** — 위임전결 한도, 예산 항목, 조달 경로
4. **인터뷰 인용 정보 일부 미기록** — 20곳 정도에 문의(2026년 8월 둘째 주)했고 심층 2건을 남겼다.
   ②는 **수원교육지원청 특수교육지원센터**로 확인됐고, **①의 기관명과 유형별 곳 수가 아직 없다**
5. 서식 번호를 확인한 지역은 강원·경남 둘뿐
6. `FORM_SOURCE`의 서식 배포 경로(신청처에서 받는지 온라인인지)는 지역마다 달라 `verified: false`

### ✅ 확인 끝난 것

- **복지·의료 트랙 4개 검증 완료** (2026.08) — 발달재활서비스 바우처 · 장애아동수당 ·
  청소년 발달장애인 방과후활동서비스 · 장애 진단·장애인 등록.
  전부 `verified: true`이고 출처 URL과 `asOf`가 붙어 있다
- **담당자 인터뷰** — 소관 밖 절차·시기를 정확히 알지는 못하고 대부분 다른 곳에 문의하라고
  전달한다. 다른 소관 자료(진단서)가 없는 채로 문의·신청이 들어와 되돌려 보내는 일이 잦다

### 조사에서 나온 것 — 이 도구의 존재 이유

**만 9세에 두 부처의 지원이 같이 끝난다.** 교육청 쪽은 발달지체 특수교육대상자 지위가
만 9세가 되는 달 말일에 종료되고, 복지부 쪽은 장애 등록 없이 발달재활서비스를 받고 있으면
만 9세가 되는 달까지만 지원된다. 소관이 달라 어느 쪽도 다른 쪽을 알려주지 않는다.
`age9Cross` 경고로 넣었고 안내문에도 두 줄로 들어간다.

**「청소년」이라는 이름 때문에 틀리게 잡아 뒀던 것** — 방과후활동서비스를 중·고등학생만으로
제한했는데 실제로는 만 6세부터다. 대신 등록된 지적·자폐성 장애인만 대상이라
발달지체 아동에게는 뜨지 않아야 한다. 둘 다 고쳤다.

## 문서 지도

| 문서 | 내용 |
|---|---|
| [docs/복지그루-기획서.md](docs/복지그루-기획서.md) | **메인.** 문제 정의 · 신청 절차 구조도 · 솔루션 · B2G 사업 모델 · 예상 질문 |
| [docs/복지그루-기획서-요약.md](docs/복지그루-기획서-요약.md) | 발표용 발췌. **근거는 기획서가 기준** |
| [docs/강원-경남-지침-분석.md](docs/강원-경남-지침-분석.md) | 두 시도 지침 원문 대조 작업 기록 |
| [docs/담당자-인터뷰.md](docs/담당자-인터뷰.md) | **현장 근거 ①.** 5문항 원본 기록 · 무엇이 반증됐는지 |
| [docs/담당자-인터뷰-수원.md](docs/담당자-인터뷰-수원.md) | **현장 근거 ②.** 수원교육지원청 지원센터 8문항 · ★ 주민센터 오안내 사례 |
| [docs/기관-문의-기록.md](docs/기관-문의-기록.md) | 문의 20곳 전체 · 인용을 합치지 않는 규칙 |
| [docs/복지그루-구조.md](docs/복지그루-구조.md) | **구조.** 역할 분담(규칙/AI/담당자) · 데이터 흐름도 · 기술 구성 · 데이터 취급 |
| [docs/발표-대본.md](docs/발표-대본.md) | **발표 대본.** 초 배분 · 말할 문장 · 뺀 것과 이유 · 예상 질문 답 |

**근거 수치를 인용할 때 출처 표기를 확인할 것.** 확인 못 한 것은 발표에서 단정하지 않는다.

---

## 실행

```bash
npm install
```

```bash
npm run dev
```

브라우저에서 http://localhost:3000

### 환경변수

| 이름 | 용도 |
|---|---|
| `GEMINI_API_KEY` | 필수. 없으면 AI 버튼이 안내 메시지를 낸다 |
| `GEMINI_MODEL` | 선택. 기본값 `gemini-3-flash-preview` |

**키가 없어도 화면과 규칙 계산은 그대로 동작한다.** AI는 버튼을 눌렀을 때만 호출된다.

## 기술 구성

Next.js 15 (App Router) · React 19 · TypeScript. **새 npm 의존성 없음** (SDK 대신 `fetch`, 검증도 손으로)

| 파일 | 하는 일 |
|---|---|
| `app/lib/data.ts` | 지역 · 장애영역 · 학교급 · 신청 상황 · 서식 · 제도 데이터. 확인한 제도에는 `officialUrl`(학부모가 열어 볼 공식 페이지)이 붙는다 |
| `app/lib/build-sheet.ts` | **규칙 계층.** 조건으로 제도·서류·기한을 고른다. 순수 함수 |
| `app/lib/validate.ts` | 요청 본문 검증. 모르는 id는 통과시키지 않는다 |
| `app/lib/board.ts` | 알림 마당 · 서식 자료실 · FAQ 데이터 (공지·파일은 가상) |
| `app/lib/screen.ts` | 화면 상수 (REPO · STEPS · FIGURES · QUICK · 나이 구간)와 표현용 헬퍼 |
| `app/lib/demo.ts` | ⚠ **데모용.** 조건 입력을 빠르게 바꾸는 가상 사례 7개. 발표 후 파일째 삭제 |
| `app/page.tsx` | 상담 화면 본체. 규칙 모듈을 브라우저에서 직접 부른다 |
| `app/components/` | 화면 조각 15개 |
| `app/globals.css` | 스타일. 색·간격은 `:root` 토큰으로만 쓰고 인라인 hex를 넣지 않는다 |

`page.tsx`에는 최상위 선언이 `Home` 하나만 있다. 데이터·상수·화면 조각은 위 모듈로 나갔다.
새 상수를 화면에 쓸 때는 `page.tsx` 안에 두지 말고 `screen.ts`에 넣는다.

| 컴포넌트 | props |
|---|---|
| `Icon` · `NoticeBoard` · `Faq` · `SiteFooter` | 없음 (또는 모듈 상수만) |
| `Reference` | `regionId` |
| `ProgramItem` | `program` |
| `DeadlineTable` | `deadlines` · `urgentCount` |
| `TestTable` | `disability` · `detailNote` |
| `LookupPanel` | `regionId` · `cache` · `busy` · `error` · `onLookup` |
| `LetterPanel` | `letter` · `isAi` · `aiStatus` · `aiError` · `copied` + 콜백 3개 |
| `ProgramList` | `sheet` · `visible` · `counts` · `viewMode` · `trackFilter` + 콜백 2개 + `children` |

**섹션이 아니라 데이터 단위로 쪼갰다.** 1단계·2단계를 섹션째 떼면 상태 27~28개를 props로
넘겨야 해서 오히려 읽기 어려워진다. `ProgramList`가 「AI 빈칸 찾기」를 `children`으로 받는 것도
같은 이유다 — DOM 구조(`.block` 안에 들어간다)를 바꾸지 않으려고 그렇게 했다.

### 백엔드 라우트

| 라우트 | 하는 일 | AI |
|---|---|---|
| `POST /api/sheet` | 조건 → 확인 시트 한 벌. 교육청 시스템이 붙는 자리 | ❌ |
| `GET /api/options` | 넣을 수 있는 id 목록 | ❌ |
| `GET /api/reference` | 지역별 카드 명칭 · 서식 번호 대조표 | ❌ |
| `GET /api/notices` | 알림 마당 · 서식 자료실 · FAQ | ❌ |
| `GET /api/health` | 배포 확인. 키 값은 응답에 넣지 않는다 | ❌ |
| `POST /api/letter` | 안내문 문장화 | ⭕ |
| `POST /api/lookup` | 빈칸 찾기 | ⭕ |

명세는 [docs/백엔드-API.md](docs/백엔드-API.md).

**화면과 API가 같은 규칙 모듈을 쓴다.** 그래서 두 결과가 어긋날 수 없다.
화면이 규칙을 브라우저에서 돌리는 이유는 두 가지다 — 조건을 바꿀 때 왕복이 없고,
아동 조건이 서버로 나가지 않는다. 이 구조를 바꾸려면 화면의 개인정보 안내 문구도 함께 고쳐야 한다.

### 배포 — Vercel

`vercel.json`을 두지 않는다. Next.js 프로젝트는 Vercel이 그대로 알아본다.
환경변수는 Project → Settings → Environment Variables 에 넣고 **다시 배포**해야 반영된다.
올린 뒤 `/api/health`로 확인한다. AI 라우트에는 `maxDuration`을 명시했다
(제미나이 응답이 Vercel 기본 한도보다 오래 걸린다).

## 작업 규칙

- 커밋 메시지는 한국어로, `docs:` `feat:` `fix:` `refactor:` 접두어 사용
- 문서를 고칠 때 **근거 수치와 출처 링크를 함께** 남긴다
- 데이터를 추가할 때 `verified` 플래그와 `source`를 반드시 채운다. 확인 못 했으면 `false`
- 나이 계산은 **오늘 날짜를 쓰지 않는다.** `BASE_DATE`(학년도 시작) 기준으로 계산한다.
  오늘 날짜를 쓰면 서버와 브라우저 렌더링이 어긋난다
- **데모용 코드는 한곳에 모아 둔다.** 발표(2026-08-28) 후 지울 것은 이 네 가지다 —
  `app/lib/demo.ts` 삭제 · `app/components/DemoPresets.tsx` 삭제 ·
  `page.tsx`의 두 import와 `<DemoPresets>` 한 줄 · `globals.css`의 `.preset*`
- **데모 사례는 조건 입력 표 안에 두지 않는다.** 실제 업무 화면에는 없는 칸이라 표에 끼우면
  담당자가 볼 화면이 아니게 된다. 화면 오른쪽에 접히는 레일(`DemoPresets`)로 띄우고,
  열려 있는 동안 `body`에 `padding-right`를 줘서 본문을 가리지 않게 한다
- **발표용 초록 주석은 걷어냈다.** `DemoKey` 컴포넌트 · `DEMO_TERMS` 용어 목록 ·
  `.demo-*` / `.keys-off` CSS · util-bar 의 「발표 키워드」 토글이 전부 사라졌다.
  화면에 초록 글씨를 다시 넣지 않는다. 쓰이지 않게 된 `--go` 토큰 3개도 함께 지웠으므로
  남은 색은 남색·파랑·회색에 경고(붉은색) · 보호자 몫(호박색) 둘뿐이다
- **커밋 전에 `npm run typecheck`를 돌린다.** Vercel 빌드는 타입 오류에서 멈춘다
